#/usr/bin/bash
DATESTAMP=$(date +'Daily_%Y%m%d')
README="${DATESTAMP}/README.md"
TS_FILE="${DATESTAMP}/${DATESTAMP}.ts"

mkdir -p $DATESTAMP
touch $README
touch $TS_FILE
cp DEFAULT_TSCONFIG.json $DATESTAMP/tsconfig.json